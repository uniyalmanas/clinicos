# DocSphere / ClinicOS Web Frontend Environment Configuration

# Backend API Base URL (defaults to http://localhost:8000 for local development)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase Client Configuration (Optional: for direct client-side Supabase Storage / Auth)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
